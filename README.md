# URL to QR Code Generator 🚀
A simple Python tool to turn any link into a scannable QR code.

## How to use
1. Install dependencies: `pip install -r requirements.txt`
2. Run the script: `python main.py`
3. Enter your URL and find your `qrcode.png` in the folder!
